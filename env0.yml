version: 1

deploy:
  steps:
    terraformInit:
      before:
        - rm -rf ${ENV0_ROOT_DIR}/sandbox-sharedsvcs/ap-southeast-1/sandbox/vpc/.terragrunt-cache
    terraformPlan:
      after:
        - jq . ${ENV0_ROOT_DIR}/sandbox-sharedsvcs/ap-southeast-1/sandbox/vpc/terragrunt_rendered.json
